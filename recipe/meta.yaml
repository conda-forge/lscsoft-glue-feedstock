{% set name = "lscsoft-glue" %}
{% set version = "1.59.0" %}
{% set sha256 = "533d786a4c5fb8a4c1d8fd370b9dda7ffb8cc6d0e498be060e5f9827e43076aa" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python -m pip install --no-deps --ignore-installed .
  skip: true  # [win]

requirements:
  build:
    - python
    - setuptools
    - pip
  run:
    - python
    - six
    - pyopenssl
    - numpy

test:
  imports:
    - glue
    - glue.ligolw
    - glue.segments

about:
  home: https://git.ligo.org/lscsoft/{{ name }}
  license: GPLv3
  license_family: GPL
  license_file: LICENSE
  summary: Grid LSC User Engine

extra:
  recipe-maintainers:
    - duncanmmacleod
    - rpfisher
